@using Cinnamon.Web.Models.Entities;
@namespace Cinnamon.Web.Components.Pages.Customer.Profile

<article class="ongoing-experience-wrapper py-3">
    <h3 class="title">Ongoing Experiences</h3>
    <div class="ongoing-experiences-containers" Margin="Margin.Is3.FromTop">
        <div class="customer-wrapper">
            @if (Activities != null)
            {
                foreach (var item in Activities)
                {
                    <OngoingExperienceItem ActivityItemType="OngoingExperienceItem.ItemType.Ongoing" Activity="item" Profile="Profile"/>
                }
            }
        </div>
    </div>
</article>


@code
{
    [Parameter]
    public IEnumerable<Activity> Activities { get; set; }
    [Parameter]
    public CustomerProfile Profile { get; set; }
}